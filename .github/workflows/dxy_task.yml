name: Dinxiangyuan1renwuguanli
on:
  workflow_dispatch:
  schedule:
    # ✅ 北京时间：每天8:13 → 次日0:13，每隔1小时运行一次
    # 转换逻辑：北京时间 = UTC时间 + 8小时
    # UTC 0:13 → 北京8:13（开始）
    # UTC 16:13 → 北京0:13（最后一次，在0:30截止前）
    # 共运行17次：8:13,9:13,10:13,...,23:13,0:13
    - cron: '13 0-16 * * *'

jobs:
  run-dxy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Cache Playwright Browsers
        uses: actions/cache@v4
        id: cache-playwright
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            pip install requests playwright
          fi
          if [ "${{ steps.cache-playwright.outputs.cache-hit }}" != 'true' ]; then
            python -m playwright install --with-deps chromium
          fi

      - name: Run DXY Script
        id: read_task
        continue-on-error: true
        run: |
          echo "========== 开始执行丁香园阅读任务 =========="
          touch output.log
          python -u dxy_read.py 2>&1 | tee output.log
        env:
          GITHUB_ACTIONS: true
          TZ: Asia/Shanghai
          DXY_COOKIE: ${{ secrets.DXY_COOKIE }}
          SCKEY: ${{ secrets.SCKEY }}

      - name: Display Full Log
        if: always()
        run: |
          echo "========== 完整执行日志 =========="
          cat output.log

      - name: Send Notification via ServerChan
        if: always()
        run: |
          if [ "${{ steps.read_task.outcome }}" = "success" ]; then
            STATUS="✅ 任务成功"
          else
            STATUS="❌ 任务失败"
          fi
          
          LOG=$(tail -n 1000 output.log | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/\\n/g')
          TITLE="丁香园自动阅读 - $STATUS"
          CONTENT="### 执行结果\n$STATUS\n\n### 日志摘要\n\`\`\`\n$LOG\n\`\`\`"
          
          if [ -n "$SCKEY" ]; then
            curl -s "https://sctapi.ftqq.com/$SCKEY.send" \
              -d "title=$TITLE" \
              -d "desp=$CONTENT"
            echo "通知已发送"
          else
            echo "未配置SCKEY，跳过通知"
          fi
        env:
          SCKEY: ${{ secrets.SCKEY }}
